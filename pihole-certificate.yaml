apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pihole-cert
  namespace: pihole
spec:
  secretName: pihole-tls
  duration: 8760h
  renewBefore: 720h
  issuerRef:
    name: pihole-selfsigned
    kind: ClusterIssuer
  dnsNames:
    - pihole.local
  ipAddresses:
    - 192.168.15.4
